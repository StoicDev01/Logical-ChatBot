import { Category } from './category';
interface ClassifierOptions {
    percentualReturn?: boolean;
}
export declare class Classifier {
    categories: Category[];
    options?: ClassifierOptions;
    constructor(options?: ClassifierOptions);
    learn(sentence: string, classifications: string[]): void;
    private normalizeData;
    private getUnknownScore;
    classify(sentence: string): Record<string, string | number>;
    getTokens(): string[];
    getCateforieByName(name: string): Category | undefined;
}
export {};
