interface TokenProps {
    strength: number;
}
export declare class TokensList {
    category: string;
    tokens: Map<string, TokenProps>;
    constructor(category: string);
    strengthenKeyword(token: string): void;
    weakenKeyword(token: string): void;
    getSimilarity(value: string): number;
    test(token: string): number;
}
export {};
